import type { ReactNode } from 'react';
import { NavLink } from 'react-router-dom';

function AdminLink({
  children,
  to,
  disabled,
}: {
  children: ReactNode;
  to: string;
  disabled?: boolean;
}) {
  return (
    <div>

    </div>
  )
}

export default AdminLink
